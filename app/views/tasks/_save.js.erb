$("ul.errors").html("")
<% if @task.errors.any? %>
  <% @task.errors.full_messages.each do |message| %>
    $("ul.errors").append($("<li />").html("<%= message.html_safe %>"))
  <% end %>
<% else %>
  $("#task-modal").modal("fade")
  $(".task-index").html("<%= j (render @tasks) %>")
<% end %>